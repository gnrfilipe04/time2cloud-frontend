# URL da API vista pelo navegador (`npm run build` / `.env.production`).
#
# Docker Compose (time2cloud-backend): o compose lê VITE_API_URL do `.env` na pasta
# do backend para o build-arg. Se não estiver lá, vale este arquivo (.env.production).
# VITE_API_URL=http://localhost:3000
